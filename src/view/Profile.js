import React from "react";
import { Button } from "react-bootstrap";

function Profile() {
  return (
    <div>
      Profile
      <div>
        <Button>Logout</Button>
      </div>
    </div>
  );
}

export default Profile;
